<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="android:colorControlNormal">@color/colorAccent</item>-->
        <item name="android:actionBarSize">@dimen/action_bar_size</item>
        <item name="actionBarSize">@dimen/action_bar_size</item>
    </style>
    <style name="AppTheme.MyDialog" parent="@style/Theme.AppCompat.Light.Dialog.Alert">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="CollectRecordTextStyle" parent="@style/ZMWLayout">
        <item name="android:textSize">14sp</item>
        <item name="android:gravity">center_vertical</item>
    </style>
</resources>
